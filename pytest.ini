[pytest]
markers =
    acceptance: End-to-end acceptance tests — app is whole when these pass
    integration: Tests that hit real external services (skipped in CI without credentials)
    e2e: Playwright browser tests — require a running Streamlit process
